/**
 * Обратная совместимость: каноничные типы и массивы — в `./products`.
 */
export type { DemoProduct, ProductStatus } from './products'
export { initialDemoProducts, PRODUCT_CATEGORIES, productStatusLabel } from './products'
